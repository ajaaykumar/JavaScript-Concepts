// Primative data types (call by value)
// 7 types: String, Number, Boolean, null, undefined, Symbol, BigInt
// Symbol: to make any value unique

let score = 100
score = 101 // We can reassign the value of score using let
console.log(`score: ${score}`)

const value = 5456
value = 221545 // This will give an error, because we cannot reassign a constant variable
console.log(value);

// Reference Type (None Primitive)
// Array, Objects, Functions






